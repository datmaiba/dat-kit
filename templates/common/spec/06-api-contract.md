# 06 — API Contract

<!-- Public interfaces: endpoints/commands with request & response shapes, error format,
     pagination convention, versioning. Changing anything here mid-build is HIGH severity
     (see build-loop rubric) — it always stops for the user. -->
