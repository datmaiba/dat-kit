# 05 — Local Environment

<!-- How to run the project locally: prerequisites, env vars (sample values — never real secrets),
     start commands, ports, seed data. The quality-gate commands live in docs/agent-working-rules.md. -->
