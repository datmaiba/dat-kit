# 03 — Data Schema

<!-- Every table/collection: fields, types, nullability, indexes, relations, cascade rules.
     Invariants the code must never violate. Migrations are generated FROM this file —
     if code and this file disagree, one of them is wrong and it's usually the code. -->
