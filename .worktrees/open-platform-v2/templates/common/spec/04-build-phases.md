# 04 — Build Phases

<!-- One phase = one session (or a few). Each phase runs the full build-loop.
     A phase is DONE only when its gates pass and its demo step works.
     Per phase: Scope (files/areas owned — grants edit permission), Demo (how to prove it works). -->

## Phase 0 — Bootstrap
Scope:
Demo:

## Phase 1 —
Scope:
Demo:
